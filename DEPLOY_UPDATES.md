# 🚀 دليل رفع التحديثات إلى PythonAnywhere

## 📋 ملخص التحديثات الجديدة

### ✅ جدول الحلقات
- إضافة حقول: الفترة، الوقت، أيام الراحة، القسم، الجنس
- رفع ملف Excel بالتنسيق الجديد
- حذف جميع البيانات
- عرض جدول كامل للمعلمين

### ✅ صفحة إعدادات الحساب
- تغيير كلمة السر من داخل النظام
- تغيير رقم الهوية من داخل النظام
- عرض معلومات الحساب

### ✅ تحسينات الواجهة
- تحديث الأيقونة (Favicon) لتتغير مع الشعار
- تحديث نص الفوتر
- إصلاح عرض البيانات في صفحة الاستعلام
- إصلاح عرض البيانات لدى المشرف

---

## 🎯 خطوات الرفع

### المرحلة 1️⃣: حفظ على GitHub

في Terminal/PowerShell على جهازك:

```bash
# الانتقال لمجلد المشروع
cd "c:/Users/abdullah/Desktop/halaqat system"

# إضافة جميع الملفات
git add .

# عمل commit مع رسالة واضحة
git commit -m "Add schedules table, account settings, and UI improvements"

# رفع على GitHub
git push origin main
```

**إذا ظهر خطأ "rejected":**
```bash
git pull origin main --rebase
git push origin main
```

---

### المرحلة 2️⃣: التحديث على PythonAnywhere

#### أ) فتح Console:
1. سجّل دخول: https://www.pythonanywhere.com
2. اذهب إلى: **Consoles**
3. افتح: **Bash console**

#### ب) تحديث الكود:
```bash
# الذهاب لمجلد المشروع
cd ~/halaqat-management-system

# سحب التحديثات من GitHub
git pull origin main
```

#### ج) تحديث قاعدة البيانات:
```bash
# إضافة الحقول الجديدة
python3 update_db.py

# إصلاح الإشعارات
python3 fix_notifications.py

# اختبار سريع
python3 diagnose.py
```

#### د) إعادة تحميل الموقع:
1. اذهب إلى تبويب **Web**
2. اضغط الزر الأخضر الكبير: **"Reload halaqat.pythonanywhere.com"**
3. انتظر 10-15 ثانية

---

### المرحلة 3️⃣: الاختبار

افتح الموقع: `https://halaqat.pythonanywhere.com`

#### اختبر:
- ✅ تسجيل الدخول
- ✅ الأيقونة تظهر بشكل صحيح
- ✅ الفوتر محدّث
- ✅ جدول الحلقات يعمل
- ✅ رفع Excel يعمل
- ✅ إعدادات الحساب تعمل
- ✅ الاستعلام يعرض البيانات

---

## 📝 الأوامر الكاملة (نسخ ولصق)

### على جهازك (Windows):
```powershell
cd "c:/Users/abdullah/Desktop/halaqat system"
git add .
git commit -m "Update: schedules table, account settings, favicon fix"
git push origin main
```

### على PythonAnywhere (Bash Console):
```bash
cd ~/halaqat-management-system
git pull origin main
python3 update_db.py
python3 fix_notifications.py
echo "✅ التحديثات جاهزة! اذهب لتبويب Web واضغط Reload"
```

---

## ⚠️ حل المشاكل الشائعة

### 1. خطأ: "git pull failed"
```bash
cd ~/halaqat-management-system
git status
git stash  # حفظ التغييرات المحلية
git pull origin main
git stash pop  # استرجاع التغييرات
```

### 2. خطأ: "Module not found"
```bash
pip3 install --user -r requirements.txt
```

### 3. خطأ: "Database locked"
```bash
# في PythonAnywhere Console:
cd ~/halaqat-management-system
rm halaqat.db
python3 setup.py
```

### 4. الأيقونة لا تتغير في المتصفح
```
- امسح الكاش (Cache): Ctrl+Shift+Delete
- أو افتح نافذة خاصة (Incognito)
- أو اضغط Ctrl+F5 لإعادة التحميل الكامل
```

---

## 🎨 تغيير الشعار والأيقونة

### لتغيير الشعار:
1. ارفع الصورة في PythonAnywhere:
   - **Files** → `halaqat-management-system/static/images/`
   - اضغط **Upload a file**
   - اختر الصورة الجديدة

2. غيّر الشعار من:
   - **الإدارة** → **تخصيص المظهر**
   - ارفع الشعار الجديد
   - احفظ

3. الأيقونة ستتغير تلقائياً! ✅

---

## ✅ قائمة المراجعة النهائية

قبل Reload تأكد من:

- [ ] `git push` نجح على GitHub
- [ ] `git pull` نجح على PythonAnywhere
- [ ] `python3 update_db.py` نجح
- [ ] `python3 fix_notifications.py` نجح
- [ ] لا توجد أخطاء في Console

بعد Reload:

- [ ] الموقع يفتح بدون أخطاء
- [ ] تسجيل الدخول يعمل
- [ ] الأيقونة ظاهرة
- [ ] جدول الحلقات يعمل
- [ ] إعدادات الحساب تعمل

---

## 📞 إذا واجهت مشكلة

### افحص Error Log:
1. **Web** tab
2. انزل لـ **Log files**
3. افتح: `halaqat.pythonanywhere.com.error.log`
4. انسخ آخر 20 سطر

### أو شغّل Diagnostic:
```bash
cd ~/halaqat-management-system
python3 diagnose.py
```

---

**جاهز للرفع! 🚀**
