# مثال على ملف متغيرات البيئة
# انسخ هذا الملف إلى .env وعدّل القيم

# المفتاح السري - استخدم مفتاح عشوائي قوي
SECRET_KEY=change-this-to-a-random-secret-key-123456789

# بيئة Flask
FLASK_ENV=production

# قاعدة البيانات
# SQLite (افتراضي)
DATABASE_URL=sqlite:///halaqat.db

# MySQL (للإنتاج)
# DATABASE_URL=mysql://username:password@localhost/halaqat_db

# PostgreSQL
# DATABASE_URL=postgresql://username:password@localhost/halaqat_db

# اسم النظام (اختياري)
SYSTEM_NAME=نظام إدارة معلمي الحلقات

# المنفذ (اختياري)
PORT=5000
