# 📝 خطوات الرفع على GitHub و PythonAnywhere

## ✅ تم التجهيز

تم تنظيف المشروع وإزالة:
- ✅ قاعدة البيانات المحلية (halaqat.db)
- ✅ ملفات __pycache__
- ✅ تحديث .gitignore
- ✅ تغيير المفتاح السري إلى قيمة عامة
- ✅ إضافة ملفات التوثيق

---

## 🌟 الخطوة 1: رفع المشروع على GitHub

### أ) إنشاء مستودع على GitHub

1. اذهب إلى https://github.com
2. سجل دخول أو أنشئ حساب
3. اضغط على الزر الأخضر **New** (أو أيقونة +)
4. املأ التفاصيل:
   - **Repository name:** `halaqat-management-system`
   - **Description:** نظام إدارة معلمي الحلقات - مكة المكرمة
   - اختر **Public** أو **Private**
   - **لا تحدد** "Initialize with README"
5. اضغط **Create repository**

### ب) رفع الكود

افتح PowerShell أو Command Prompt في مجلد المشروع ونفذ:

```bash
# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# أول commit
git commit -m "نظام إدارة معلمي الحلقات - النسخة 2.0 - إصدار أولي"

# تسمية الفرع الرئيسي
git branch -M main

# ربط المستودع (استبدل YOUR_USERNAME باسم المستخدم)
git remote add origin https://github.com/YOUR_USERNAME/halaqat-management-system.git

# رفع الكود
git push -u origin main
```

> **ملاحظة:** قد يطلب منك GitHub اسم المستخدم وكلمة المرور. استخدم **Personal Access Token** بدلاً من كلمة المرور.

### ج) إنشاء Personal Access Token (إذا لزم الأمر)

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. اختر الصلاحيات: `repo`
4. انسخ التوكن واستخدمه بدلاً من كلمة المرور

---

## 🌐 الخطوة 2: النشر على PythonAnywhere

### أ) التسجيل

1. اذهب إلى https://www.pythonanywhere.com
2. اضغط **Pricing & signup**
3. اختر **Create a Beginner account** (مجاني)
4. املأ البيانات وفعّل الحساب

### ب) إعداد المشروع

1. **افتح Bash Console:**
   - من لوحة التحكم → Consoles → Bash

2. **استنسخ المشروع:**
```bash
git clone https://github.com/YOUR_USERNAME/halaqat-management-system.git
cd halaqat-management-system
```

3. **إنشاء بيئة افتراضية:**
```bash
mkvirtualenv --python=/usr/bin/python3.10 halaqat-env
```

4. **تثبيت المتطلبات:**
```bash
pip install -r requirements.txt
```

5. **إعداد قاعدة البيانات:**
```bash
python setup.py
```

### ج) إعداد Web App

1. **إنشاء Web App:**
   - اذهب لتبويب **Web**
   - اضغط **Add a new web app**
   - اضغط **Next**
   - اختر **Manual configuration**
   - اختر **Python 3.10**

2. **تكوين WSGI:**
   - في صفحة Web، اضغط على **WSGI configuration file**
   - احذف المحتوى واستبدله بـ:

```python
import sys
import os

# استبدل YOUR_USERNAME باسم المستخدم الخاص بك
project_home = '/home/YOUR_USERNAME/halaqat-management-system'
if project_home not in sys.path:
    sys.path.insert(0, project_home)

# إعداد متغيرات البيئة
os.environ['SECRET_KEY'] = 'اكتب-مفتاح-سري-قوي-هنا-1234567890'
os.environ['FLASK_ENV'] = 'production'

from app import app as application
```

3. **تعيين البيئة الافتراضية:**
   - في صفحة Web، قسم **Virtualenv**
   - أدخل: `/home/YOUR_USERNAME/.virtualenvs/halaqat-env`

4. **إعداد الملفات الثابتة:**
   - في قسم **Static files**، اضغط **Add a new static file**:
   
   | URL | Directory |
   |-----|-----------|
   | `/static/` | `/home/YOUR_USERNAME/halaqat-management-system/static/` |
   | `/uploads/` | `/home/YOUR_USERNAME/halaqat-management-system/uploads/` |

5. **إعادة تحميل التطبيق:**
   - اضغط الزر الأخضر **Reload YOUR_USERNAME.pythonanywhere.com**

### د) اختبار التطبيق

1. افتح `https://YOUR_USERNAME.pythonanywhere.com`
2. يجب أن تظهر الصفحة الرئيسية
3. سجل دخول بـ:
   - رقم الهوية: `1000000000`
   - كلمة المرور: `admin123`

⚠️ **مهم جداً:** غيّر كلمة المرور فوراً!

---

## 🔐 خطوات الأمان بعد النشر

### 1. تغيير كلمة مرور المدير

من Bash Console على PythonAnywhere:
```bash
cd ~/halaqat-management-system
python change_admin_password.py
```

### 2. تعيين مفتاح سري قوي

في ملف WSGI، استبدل:
```python
os.environ['SECRET_KEY'] = 'اكتب-مفتاح-سري-قوي-هنا-1234567890'
```

بمفتاح عشوائي قوي. يمكنك توليد واحد:
```python
import secrets
print(secrets.token_urlsafe(32))
```

### 3. مراجعة الصلاحيات

```bash
cd ~/halaqat-management-system
chmod 644 halaqat.db
chmod -R 755 static/
chmod -R 755 uploads/
```

---

## 🔄 التحديثات اللاحقة

عندما تريد تحديث الموقع:

### على GitHub:
```bash
git add .
git commit -m "وصف التحديث"
git push
```

### على PythonAnywhere:
```bash
cd ~/halaqat-management-system
git pull origin main
# ثم اضغط Reload في تبويب Web
```

---

## ⚠️ ملاحظات مهمة

### حدود الحساب المجاني:

- ✅ تطبيق واحد على `username.pythonanywhere.com`
- ✅ قاعدة بيانات SQLite
- ⚠️ حركة مرور محدودة
- ⚠️ CPU محدود
- ❌ لا يدعم HTTPS على النطاقات المخصصة

### للترقية:
- نطاق مخصص: يتطلب حساب مدفوع
- قاعدة MySQL: متاحة في الحساب المجاني
- أداء أفضل: ترقية للحساب المدفوع

---

## 📊 مراقبة التطبيق

### سجلات الأخطاء:
```bash
tail -f /var/log/YOUR_USERNAME.pythonanywhere.com.error.log
```

### سجل الخادم:
```bash
tail -f /var/log/YOUR_USERNAME.pythonanywhere.com.server.log
```

---

## 🆘 حل المشاكل الشائعة

### خطأ 500 - Internal Server Error
1. راجع سجل الأخطاء
2. تأكد من تثبيت جميع المتطلبات
3. تحقق من صحة مسار WSGI

### الصفحة بيضاء فارغة
1. تأكد من إعادة تحميل التطبيق
2. راجع ملف WSGI
3. تحقق من البيئة الافتراضية

### الصور لا تظهر
1. تحقق من إعدادات Static files
2. راجع صلاحيات المجلدات
3. امسح ذاكرة المتصفح (Ctrl+Shift+R)

---

## ✅ قائمة التحقق النهائية

قبل الإطلاق الرسمي:

- [ ] رفع المشروع على GitHub
- [ ] نشر على PythonAnywhere
- [ ] تغيير كلمة مرور المدير
- [ ] تعيين SECRET_KEY قوي
- [ ] اختبار تسجيل الدخول
- [ ] اختبار جميع الصفحات
- [ ] اختبار رفع الملفات
- [ ] إعداد النسخ الاحتياطي
- [ ] مراجعة سجلات الأخطاء
- [ ] إضافة بيانات حقيقية

---

## 🎉 تم بنجاح!

**GitHub Repository:** `https://github.com/YOUR_USERNAME/halaqat-management-system`

**Live Website:** `https://YOUR_USERNAME.pythonanywhere.com`

**بيانات الدخول الافتراضية:**
- رقم الهوية: `1000000000`
- كلمة المرور: `admin123` (غيّرها فوراً!)

---

## 📞 الدعم والمساعدة

- **GitHub Issues:** للإبلاغ عن المشاكل
- **PythonAnywhere Forum:** للمساعدة التقنية
- **التوثيق:** راجع `README.md` و `PYTHONANYWHERE_DEPLOYMENT.md`

---

**تاريخ الإنشاء:** 2025-01-06  
**الإصدار:** 2.0.0  
**الحالة:** ✅ جاهز للنشر

**بالتوفيق! 🚀**
